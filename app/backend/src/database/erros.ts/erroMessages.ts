export const emailOrPasswodInvalid = { message: 'Incorrect email or password' };

export const joiMessageErro = (err: any) => ({ message: err.message });

export const incorrectEmailOrPassword = { message: 'Incorrect email or password' };

export const fildsMustBeFilled = { message: 'All fields must be filled' };

export const isNotPossibleTwoEqualTeams = (
  { message: 'It is not possible to create a match with two equal teams' });

export const teamNotFound = { message: 'There is no team with such id!' };
